#include <stdio.h>
int main(void)
{
    float x,y, xx, yy;
    scanf("%f %f", &x, &y);
    if(x<10)
        x = x/10;
    else
    {
        for(int i=1; i<=x; i)
        {
            
        }
    }
    if(y<10)
        y = y/10;
    
    printf("%f", x+y);
}